{% extends 'base.html.twig' %}

{% block title %}{{ type }}{% endblock %}
{% block body %}
<h1 class="title"> Informations générales concernant les {{ type }} : </h1>
<br/>
<div class="content">
    <div class="numbers-players">
        <div class="big-number">
        {% if type == 'heroes' %}
            <img src="{{ asset('build/images/blaster.png') }}" class="img-big-number">
        {% else %}
            <img src="{{ asset('build/images/spaceship.png') }}" class="img-big-number">
        {% endif %}
            <br/> {{ number }} {{ type }}
        </div>
    </div>
    <div class="select-{{ type }}">
        <h2 class="h2"> Liste de tout les {{ type }} de la guilde </h2>
        <div class="form-group">
            <select class="form-control" id="selectUnit" name="{{ type }}">
                {% for unit in units %}        
                <option value="{{ unit.id }}">{{ unit.name }} ({% if app.request.pathinfo == '/heroes' %}{{ unit.heroPlayers | length }}{% else %}{{ unit.shipPlayers | length }}{% endif %})</option>
                {% endfor %}
            </select>
        </div>
    </div>
    <div id="list-ajax">
    </div>
</div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('unit') }}
{% endblock %}